# 🤖 Команды для AI Ассистента (Copilot)

## Запуск системы после исправлений

### Вариант 1 (краткий):
```
Запусти PDM систему
```

### Вариант 2 (полный):
```
Запусти серверы PDM: сначала останови все процессы, затем запусти через start_servers.ps1
```

### Вариант 3 (после изменений):
```
Перезапусти PDM систему после изменений
```

---

## Что делает ассистент

При команде **"Запусти PDM систему"** ассистент должен:

1. Остановить зависшие процессы:
   ```powershell
   Get-Process node,python -ErrorAction SilentlyContinue | Stop-Process -Force
   ```

2. Запустить систему:
   ```powershell
   cd D:\B2B\PDM
   .\start_servers.ps1
   ```

3. Проверить доступность (через 3-5 секунд):
   ```powershell
   Invoke-WebRequest -Uri "http://localhost:3000" -UseBasicParsing
   Invoke-WebRequest -Uri "http://localhost:8000/api/docs/" -UseBasicParsing
   ```

4. Открыть браузер:
   - http://localhost:3000

---

## Остановка системы

```
Останови PDM систему
```

Выполняет:
```powershell
cd D:\B2B\PDM
.\stop_servers.ps1
```

---

## Проверка статуса

```
Проверь статус PDM системы
```

Выполняет:
```powershell
cd D:\B2B\PDM
.\check_servers.ps1
```

---

## Решение ERR_CONNECTION_REFUSED

```
Исправь ошибку ERR_CONNECTION_REFUSED в PDM
```

Выполняет:
1. Остановка всех процессов
2. Запуск через start_servers.ps1
3. Проверка доступности
4. Открытие браузера

---

## Типичные сценарии

### После внесения изменений в код:

```
Я внес изменения в [файл/компонент]. Перезапусти систему.
```

### Если система не отвечает:

```
Система не отвечает. Перезапусти PDM.
```

### Если нужна полная перезагрузка:

```
Сделай полный перезапуск PDM: останови все, очисти процессы, запусти заново.
```

---

## Важные пути

- **Главная директория:** `D:\B2B\PDM`
- **Backend:** `D:\B2B\PDM\backend`
- **Frontend:** `D:\B2B\PDM\frontend`
- **Python venv:** `D:\B2B\PDM\.venv`

## Порты

- **Frontend:** 3000
- **Backend:** 8000

## Учетные данные

- **Username:** admin
- **Password:** admin123

---

## Примеры правильных запросов

✅ **"Запусти PDM систему"**  
✅ **"Перезапусти серверы"**  
✅ **"Останови PDM"**  
✅ **"Проверь статус серверов"**  
✅ **"Исправь ERR_CONNECTION_REFUSED"**  
✅ **"После изменений перезапусти систему"**

---

## Скрипты в корне проекта

| Скрипт | Назначение |
|--------|------------|
| `start_servers.ps1` | Запуск Backend + Frontend |
| `stop_servers.ps1` | Остановка всех серверов |
| `check_servers.ps1` | Проверка статуса |

---

**Примечание для ассистента:**  
Всегда используй скрипт `start_servers.ps1` для запуска, а не отдельные команды npm/python.  
Скрипт автоматически создает окна терминалов для Backend и Frontend.

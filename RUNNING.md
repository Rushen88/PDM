# PDM ERP System - Инструкция по запуску

## Предварительные требования

- Python 3.10+
- Node.js 18+
- PostgreSQL 14+
- Redis (для Celery и WebSocket)

## ⚡ Быстрый запуск (РЕКОМЕНДУЕТСЯ)

### Запуск серверов

**Вариант 1: PowerShell (рекомендуется)**
```powershell
# Из корневой директории проекта
.\start_servers.ps1
```

**Вариант 2: Batch файл**
```cmd
start_servers.bat
```

Скрипт автоматически:
- Проверит, запущены ли серверы
- Предложит перезапустить, если уже работают
- Откроет два отдельных окна для Backend и Frontend
- Проверит доступность API

### Проверка статуса серверов

```powershell
.\check_servers.ps1
```

Показывает:
- Статус Backend и Frontend процессов
- PID и время запуска
- Доступность API endpoints

### Остановка серверов

**Вариант 1: PowerShell (рекомендуется)**
```powershell
.\stop_servers.ps1
```

**Вариант 2: Batch файл**
```cmd
stop_servers.bat
```

### После запуска

- **Backend API**: http://localhost:8000
- **Frontend**: http://localhost:3000
- **API Documentation**: http://localhost:8000/api/docs/
- **Admin Panel**: http://localhost:8000/admin/

---

## 📝 Ручной запуск (если нужен больший контроль)

### 1. Настройка базы данных

```sql
-- Создать базу данных в PostgreSQL
CREATE DATABASE PDM;
```

### 2. Настройка бэкенда

```bash
# Активировать виртуальное окружение
cd d:\B2B\PDM
.venv\Scripts\activate

# Установить зависимости (если не установлены)
pip install -r backend/requirements/dev.txt

# Применить миграции
cd backend
python manage.py migrate

# Создать суперпользователя (если не создан)
python manage.py createsuperuser

# Запустить сервер
python manage.py runserver 0.0.0.0:8000
```

### 3. Настройка фронтенда

```bash
# Перейти в директорию фронтенда
cd d:\B2B\PDM\frontend

# Установить зависимости (если не установлены)
npm install

# Запустить dev-сервер
npm run dev
```

## 4. Доступ к системе

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000/api/v1/
- **API Documentation**: http://localhost:8000/api/schema/swagger-ui/
- **Django Admin**: http://localhost:8000/admin/

## Аутентификация

Система использует JWT-токены для аутентификации:

1. Войдите с учётными данными пользователя (admin/admin123 если создан)
2. Токены автоматически сохраняются в localStorage
3. При истечении access token автоматически обновляется через refresh token

## API Endpoints

### Аутентификация
- `POST /api/v1/auth/login/` - Вход в систему
- `POST /api/v1/auth/logout/` - Выход из системы
- `GET /api/v1/auth/me/` - Текущий пользователь

### Справочники
- `GET/POST /api/v1/nomenclature/` - Номенклатура
- `GET/POST /api/v1/nomenclature-types/` - Типы номенклатуры
- `GET/POST /api/v1/suppliers/` - Поставщики
- `GET/POST /api/v1/contractors/` - Подрядчики

### Структуры изделий (BOM)
- `GET/POST /api/v1/bom/` - Структуры изделий
- `GET /api/v1/bom/{id}/tree/` - Дерево структуры
- `GET/POST /api/v1/bom-items/` - Позиции BOM

### Проекты
- `GET/POST /api/v1/projects/` - Проекты
- `GET /api/v1/projects/{id}/progress/` - Прогресс проекта
- `GET /api/v1/projects/{id}/structure/` - Структура проекта
- `GET/POST /api/v1/project-items/` - Позиции проекта

## Переменные окружения

### Backend (.env)
```
DEBUG=True
SECRET_KEY=your-secret-key
DB_NAME=PDM
DB_USER=postgres
DB_PASSWORD=your-password
DB_HOST=127.0.0.1
DB_PORT=5432
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
```

### Frontend (.env.development)
```
VITE_API_URL=/api/v1
VITE_WS_URL=ws://127.0.0.1:8000/ws
VITE_APP_TITLE=PDM ERP (Dev)
```

---

## 🔧 Решение проблем

### Ошибка авторизации / "401 Unauthorized"

**Проблема**: После перезапуска сервера не удается войти в систему.

**Причины**:
1. Backend сервер не запущен
2. Backend упал после запуска
3. Неверные учетные данные

**Решение**:
```powershell
# 1. Проверить статус серверов
.\check_servers.ps1

# 2. Если Backend не работает, перезапустить
.\stop_servers.ps1
.\start_servers.ps1

# 3. Проверить окно Backend на наличие ошибок
# 4. Убедиться, что PostgreSQL запущен и доступен
```

### Backend падает сразу после запуска

**Проблема**: Backend запускается, но сразу завершается.

**Причины**:
1. Синтаксические ошибки в коде
2. Ошибки импорта модулей
3. Неправильная конфигурация

**Решение**:
```powershell
# 1. Проверить логи в окне Backend
# 2. Запустить вручную для просмотра полных ошибок:
cd d:\B2B\PDM\backend
D:\B2B\PDM\.venv\Scripts\python.exe manage.py runserver 0.0.0.0:8000

# 3. Проверить синтаксис Python файлов
# 4. Убедиться, что все зависимости установлены:
D:\B2B\PDM\.venv\Scripts\pip.exe install -r requirements/dev.txt
```

### Frontend не компилируется

**Проблема**: Frontend показывает ошибки компиляции TypeScript.

**Решение**:
```bash
cd d:\B2B\PDM\frontend

# Очистить кэш и переустановить зависимости
rm -rf node_modules
rm package-lock.json
npm install

# Проверить ошибки типов
npm run type-check
```

### CORS ошибки

**Проблема**: Браузер блокирует запросы к Backend API.

**Решение**:
1. Убедиться, что Backend запущен на `0.0.0.0:8000` (не `127.0.0.1`)
2. Проверить настройки CORS в `backend/.env`:
   ```
   CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
   ```
3. Перезапустить Backend после изменения `.env`

### Порт уже занят

**Проблема**: Ошибка "Address already in use" при запуске.

**Решение**:
```powershell
# Проверить процессы на портах
Get-NetTCPConnection -LocalPort 8000,3000 | Select-Object LocalPort, OwningProcess, State

# Остановить старые процессы
.\stop_servers.ps1

# Если не помогло, убить процессы вручную:
# Backend (Python):
Get-Process python | Stop-Process -Force

# Frontend (Node):
Get-Process node | Stop-Process -Force
```

### База данных недоступна

**Проблема**: Backend не может подключиться к PostgreSQL.

**Решение**:
```powershell
# 1. Проверить, запущен ли PostgreSQL
Get-Service postgresql*

# 2. Запустить PostgreSQL, если не запущен
# (зависит от установки)

# 3. Проверить настройки подключения в backend/.env
# 4. Проверить доступ:
psql -U postgres -d PDM

# 5. При необходимости пересоздать базу:
# createdb -U postgres PDM
# cd d:\B2B\PDM\backend
# python manage.py migrate
```

### Старые токены авторизации

**Проблема**: Система выдает ошибки после изменений в модели User.

**Решение**:
```javascript
// В консоли браузера (F12):
localStorage.clear();
// Затем перезагрузить страницу и войти заново
```

---

## 📌 Важные заметки

### После изменений в коде Backend:
1. **Изменения в моделях** → запустить `python manage.py makemigrations` и `python manage.py migrate`
2. **Изменения в views/serializers** → перезапустить Backend (сервер перезагрузится автоматически в dev режиме)
3. **Изменения в зависимостях** → переустановить пакеты: `pip install -r backend/requirements/dev.txt`

### После изменений в коде Frontend:
1. **Изменения в компонентах** → сохраняются автоматически (HMR)
2. **Изменения в зависимостях** → переустановить: `npm install`
3. **Изменения в типах** → проверить компиляцию: `npm run type-check`

### Рекомендации для стабильной работы:
1. **Всегда используйте скрипты запуска** (`start_servers.ps1`) вместо ручного запуска
2. **Проверяйте статус** перед началом работы: `check_servers.ps1`
3. **Останавливайте правильно**: `stop_servers.ps1` перед выключением компьютера
4. **Следите за окнами серверов** на наличие ошибок и предупреждений
5. **Используйте отдельные окна** для Backend и Frontend для удобного отслеживания логов

---

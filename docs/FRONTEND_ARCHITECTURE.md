# Архитектура Frontend — PDM mini-ERP
## Профессиональный UI для производственной ERP-системы

---

## 1. Философия интерфейса

### 1.1 Ключевые принципы

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         UX-ПРИНЦИПЫ СИСТЕМЫ                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ✦ INFORMATION FIRST                                                        │
│    Данные — главный элемент. Декор подчинён функции.                        │
│                                                                             │
│  ✦ ЦВЕТ = СМЫСЛ                                                             │
│    Минимум цветов. Каждый цвет несёт семантику:                             │
│    • Красный    → критично / просрочено / ошибка                            │
│    • Оранжевый  → предупреждение / риск / внимание                          │
│    • Зелёный    → выполнено / в норме / успех                               │
│    • Синий      → информация / ссылка / процесс                             │
│    • Серый      → нейтральное / неактивно                                   │
│                                                                             │
│  ✦ ПРЕДСКАЗУЕМОСТЬ                                                          │
│    Одинаковое поведение во всех модулях.                                    │
│    Пользователь не должен думать "как это работает".                        │
│                                                                             │
│  ✦ МИНИМУМ КЛИКОВ                                                           │
│    3 клика до любой информации.                                             │
│    Контекстные действия рядом с данными.                                    │
│                                                                             │
│  ✦ ГЛУБИНА БЕЗ ПОТЕРИ КОНТЕКСТА                                             │
│    Drill-down с сохранением breadcrumbs.                                    │
│    Возможность открыть в новой вкладке/панели.                              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 Целевые метрики UX

| Метрика | Целевое значение |
|---------|------------------|
| Time to first meaningful action | < 3 сек |
| Кликов до ключевых данных | ≤ 3 |
| Время обучения нового сотрудника | < 1 день |
| Время загрузки таблицы 1000 строк | < 1 сек |
| Отклик на действие пользователя | < 100 мс |

---

## 2. Технологический стек

### 2.1 Core

| Технология | Версия | Назначение |
|------------|--------|------------|
| React | 18.x | UI Framework |
| TypeScript | 5.x | Type Safety |
| Vite | 5.x | Build Tool |
| React Router | 6.x | Routing |

### 2.2 State Management

| Технология | Назначение |
|------------|------------|
| Zustand | Global State (lightweight) |
| TanStack Query | Server State, caching, sync |
| React Context | Auth, Theme, Layout state |

### 2.3 UI Framework

| Технология | Назначение |
|------------|------------|
| Ant Design 5 | Component Library |
| @ant-design/plots | Charts & Graphs |
| @ant-design/pro-components | Enterprise components |
| Tailwind CSS | Utility classes (дополнение) |

### 2.4 Специализированные библиотеки

| Технология | Назначение |
|------------|------------|
| dhtmlx-gantt или frappe-gantt | Gantt диаграммы |
| react-arborist | Tree views (BOM структура) |
| @tanstack/react-table | Продвинутые таблицы |
| react-resizable-panels | Split panels |
| date-fns | Даты (русская локаль) |
| recharts | Графики |

### 2.5 Real-time

| Технология | Назначение |
|------------|------------|
| WebSocket API | Live updates |
| socket.io-client | Fallback + reconnection |

---

## 3. Структура проекта

```
frontend/
├── public/
│   ├── favicon.ico
│   └── manifest.json
│
├── src/
│   ├── app/                          # Application setup
│   │   ├── App.tsx
│   │   ├── routes.tsx                # Route definitions
│   │   └── providers/                # Context providers
│   │       ├── AuthProvider.tsx
│   │       ├── ThemeProvider.tsx
│   │       ├── QueryProvider.tsx
│   │       └── WebSocketProvider.tsx
│   │
│   ├── pages/                        # Page components (route-level)
│   │   ├── auth/
│   │   │   └── LoginPage.tsx
│   │   ├── dashboard/
│   │   │   ├── DashboardPage.tsx
│   │   │   └── components/
│   │   ├── projects/
│   │   │   ├── ProjectListPage.tsx
│   │   │   ├── ProjectDetailPage.tsx
│   │   │   └── components/
│   │   ├── catalog/
│   │   │   ├── NomenclaturePage.tsx
│   │   │   ├── SuppliersPage.tsx
│   │   │   ├── ContractorsPage.tsx
│   │   │   └── components/
│   │   ├── bom/
│   │   │   ├── BOMListPage.tsx
│   │   │   ├── BOMEditorPage.tsx
│   │   │   └── components/
│   │   ├── procurement/
│   │   │   ├── PurchaseListPage.tsx
│   │   │   └── components/
│   │   ├── production/
│   │   │   ├── WorkOrdersPage.tsx
│   │   │   └── components/
│   │   ├── inventory/
│   │   │   ├── WarehousePage.tsx
│   │   │   └── components/
│   │   ├── analytics/
│   │   │   ├── ReportsPage.tsx
│   │   │   └── components/
│   │   └── settings/
│   │       ├── UsersPage.tsx
│   │       ├── RolesPage.tsx
│   │       └── components/
│   │
│   ├── features/                     # Feature-based modules
│   │   ├── auth/
│   │   │   ├── api.ts
│   │   │   ├── hooks.ts
│   │   │   ├── store.ts
│   │   │   └── types.ts
│   │   ├── projects/
│   │   │   ├── api.ts
│   │   │   ├── hooks.ts
│   │   │   ├── store.ts
│   │   │   └── types.ts
│   │   ├── catalog/
│   │   ├── bom/
│   │   ├── procurement/
│   │   ├── production/
│   │   ├── inventory/
│   │   └── analytics/
│   │
│   ├── shared/                       # Shared code
│   │   ├── api/
│   │   │   ├── client.ts             # Axios instance
│   │   │   ├── endpoints.ts          # API endpoints
│   │   │   └── types.ts              # Common API types
│   │   ├── components/               # Reusable UI components
│   │   │   ├── layout/
│   │   │   │   ├── AppLayout.tsx
│   │   │   │   ├── Sidebar.tsx
│   │   │   │   ├── Header.tsx
│   │   │   │   └── Breadcrumbs.tsx
│   │   │   ├── data-display/
│   │   │   │   ├── DataTable.tsx
│   │   │   │   ├── TreeTable.tsx
│   │   │   │   ├── StatusBadge.tsx
│   │   │   │   ├── ProgressBar.tsx
│   │   │   │   └── StatCard.tsx
│   │   │   ├── data-entry/
│   │   │   │   ├── SearchInput.tsx
│   │   │   │   ├── FilterPanel.tsx
│   │   │   │   └── InlineEdit.tsx
│   │   │   ├── feedback/
│   │   │   │   ├── LoadingOverlay.tsx
│   │   │   │   └── ErrorBoundary.tsx
│   │   │   └── navigation/
│   │   │       ├── TabNav.tsx
│   │   │       └── QuickActions.tsx
│   │   ├── hooks/
│   │   │   ├── useDebounce.ts
│   │   │   ├── useLocalStorage.ts
│   │   │   ├── useWebSocket.ts
│   │   │   └── useTableState.ts
│   │   ├── utils/
│   │   │   ├── format.ts
│   │   │   ├── date.ts
│   │   │   ├── validation.ts
│   │   │   └── tree.ts
│   │   ├── constants/
│   │   │   ├── status.ts
│   │   │   ├── routes.ts
│   │   │   └── roles.ts
│   │   └── types/
│   │       ├── common.ts
│   │       └── models.ts
│   │
│   ├── styles/
│   │   ├── index.css
│   │   ├── theme.ts                  # Ant Design theme config
│   │   ├── variables.css             # CSS variables
│   │   └── utilities.css
│   │
│   ├── assets/
│   │   ├── icons/
│   │   └── images/
│   │
│   └── main.tsx
│
├── .env
├── .env.development
├── .env.production
├── vite.config.ts
├── tsconfig.json
├── tailwind.config.js
├── package.json
└── README.md
```

---

## 4. Компонентная архитектура

### 4.1 Layout System

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ ▤ PDM ERP                      🔍 Search...                    👤 Admin  ▼ │
├─────────────────────────────────────────────────────────────────────────────┤
│         │                                                                   │
│ ═══════ │  📍 Проекты › СТЕНД-001 › Система ГДС › Подсистема ГДС-01        │
│         │                                                                   │
│ ▣ Панель│  ┌─────────────────────────────────────────────────────────────┐ │
│         │  │ [Обзор] [Структура] [Закупки] [Производство] [Документы]   │ │
│ ☰ Проекты  │  ├─────────────────────────────────────────────────────────────┤ │
│   ▸ Актив │  │                                                             │ │
│   ▸ Архив │  │                    CONTENT AREA                             │ │
│         │  │                                                             │ │
│ ☰ Справоч │  │   Таблицы, формы, графики, Gantt...                         │ │
│   ▸ Номен │  │                                                             │ │
│   ▸ Поста │  │                                                             │ │
│   ▸ Подря │  │                                                             │ │
│         │  │                                                             │ │
│ ☰ Закупки │  │                                                             │ │
│         │  │                                                             │ │
│ ☰ Произв  │  │                                                             │ │
│         │  │                                                             │ │
│ ☰ Склад  │  │                                                             │ │
│         │  │                                                             │ │
│ ☰ Аналит │  │                                                             │ │
│         │  │                                                             │ │
│ ━━━━━━━ │  │                                                             │ │
│ ⚙ Настр  │  │                                                             │ │
│         │  └─────────────────────────────────────────────────────────────┘ │
│ [◀]     │                                                                   │
└─────────────────────────────────────────────────────────────────────────────┘

Ширина Sidebar: 240px (свёрнут: 64px)
Header height: 56px
Content padding: 24px
```

### 4.2 Ролевые стартовые экраны

| Роль | Стартовый экран | Приоритеты информации |
|------|-----------------|----------------------|
| **Руководитель** | Dashboard с KPI | Общий прогресс, критические задержки, ресурсы |
| **Инженер** | Мои проекты | Текущие задачи, BOM структуры, статусы |
| **Снабжение** | Ведомость закупок | Сроки заказов, статусы поставок, просрочки |
| **Производство** | Производственный план | Задачи на изготовление, загрузка, материалы |
| **Склад** | Складской учёт | Остатки, резервы, движения |
| **ПДО** | Планирование | Gantt, критический путь, ресурсы |

### 4.3 Ключевые переиспользуемые компоненты

#### DataTable — Универсальная таблица данных

```typescript
interface DataTableProps<T> {
  // Data
  data: T[];
  columns: ColumnDef<T>[];
  loading?: boolean;
  
  // Features
  pagination?: PaginationConfig;
  sorting?: SortingConfig;
  filtering?: FilterConfig;
  selection?: SelectionConfig;
  
  // Columns
  resizable?: boolean;
  reorderable?: boolean;
  pinnable?: boolean;          // Закрепление колонок
  
  // Views
  savedViews?: boolean;        // Сохранение пользовательских представлений
  exportable?: boolean;        // Экспорт в Excel/CSV
  
  // Interactions
  onRowClick?: (row: T) => void;
  onRowDoubleClick?: (row: T) => void;
  rowContextMenu?: (row: T) => MenuItem[];
  
  // Styling
  rowClassName?: (row: T) => string;
  size?: 'small' | 'middle' | 'large';
  bordered?: boolean;
  striped?: boolean;
}
```

#### TreeTable — Иерархическая таблица (для BOM)

```typescript
interface TreeTableProps<T extends TreeNode> {
  data: T[];
  columns: ColumnDef<T>[];
  
  // Tree features
  defaultExpandedKeys?: string[];
  expandedKeys?: string[];
  onExpand?: (keys: string[], node: T) => void;
  loadChildren?: (node: T) => Promise<T[]>;  // Lazy loading
  
  // Selection
  checkable?: boolean;
  checkedKeys?: string[];
  onCheck?: (keys: string[], info: CheckInfo) => void;
  
  // Visual
  indentSize?: number;
  showLines?: boolean;
}
```

#### StatusBadge — Статусный индикатор

```typescript
type StatusType = 
  | 'not_started' | 'in_progress' | 'completed' | 'suspended'
  | 'pending' | 'ordered' | 'in_transit' | 'delivered' | 'delayed'
  | 'draft' | 'planning' | 'on_hold' | 'cancelled';

interface StatusBadgeProps {
  status: StatusType;
  showText?: boolean;
  showIcon?: boolean;
  size?: 'small' | 'default';
  pulsate?: boolean;           // Для критических статусов
}

// Цветовая схема статусов:
const STATUS_COLORS: Record<StatusType, string> = {
  not_started: '#8c8c8c',      // Серый
  in_progress: '#1890ff',      // Синий
  completed: '#52c41a',        // Зелёный
  suspended: '#faad14',        // Оранжевый
  delayed: '#ff4d4f',          // Красный
  // ...
};
```

#### ProgressBar — Индикатор прогресса

```typescript
interface ProgressBarProps {
  percent: number;
  target?: number;             // Целевое значение для сравнения
  showInfo?: boolean;
  size?: 'small' | 'default' | 'large';
  
  // Цветовая индикация по уровню
  thresholds?: {
    danger: number;            // < 30% — красный
    warning: number;           // < 70% — жёлтый
    success: number;           // >= 70% — зелёный
  };
}
```

---

## 5. Навигация и роутинг

### 5.1 Структура маршрутов

```typescript
const routes = {
  // Auth
  login: '/login',
  
  // Dashboard
  dashboard: '/',
  
  // Projects
  projects: '/projects',
  projectDetail: '/projects/:id',
  projectStructure: '/projects/:id/structure',
  projectGantt: '/projects/:id/gantt',
  projectProcurement: '/projects/:id/procurement',
  projectProduction: '/projects/:id/production',
  
  // Catalog
  nomenclature: '/catalog/nomenclature',
  nomenclatureDetail: '/catalog/nomenclature/:id',
  suppliers: '/catalog/suppliers',
  supplierDetail: '/catalog/suppliers/:id',
  contractors: '/catalog/contractors',
  
  // BOM
  bomList: '/bom',
  bomEditor: '/bom/:id',
  
  // Procurement
  purchaseList: '/procurement',
  purchaseSchedule: '/procurement/schedule',
  
  // Production
  workOrders: '/production/orders',
  productionSchedule: '/production/schedule',
  
  // Inventory
  warehouse: '/inventory',
  movements: '/inventory/movements',
  reservations: '/inventory/reservations',
  
  // Analytics
  reports: '/analytics/reports',
  projectReport: '/analytics/projects/:id',
  
  // Settings
  users: '/settings/users',
  roles: '/settings/roles',
  system: '/settings/system',
};
```

### 5.2 Меню навигации (по ролям)

```typescript
interface MenuItem {
  key: string;
  label: string;
  icon: ReactNode;
  path?: string;
  children?: MenuItem[];
  roles?: string[];            // Кому доступно
  badge?: number | 'dot';      // Уведомление
}

const menuItems: MenuItem[] = [
  {
    key: 'dashboard',
    label: 'Панель управления',
    icon: <DashboardOutlined />,
    path: '/',
    roles: ['admin', 'project_manager', 'engineer', 'procurement', 'production', 'warehouse'],
  },
  {
    key: 'projects',
    label: 'Проекты',
    icon: <ProjectOutlined />,
    roles: ['admin', 'project_manager', 'engineer', 'production'],
    children: [
      { key: 'projects-active', label: 'Активные', path: '/projects?status=active' },
      { key: 'projects-all', label: 'Все проекты', path: '/projects' },
      { key: 'projects-archive', label: 'Архив', path: '/projects?status=archived' },
    ],
  },
  {
    key: 'catalog',
    label: 'Справочники',
    icon: <DatabaseOutlined />,
    roles: ['admin', 'engineer', 'procurement'],
    children: [
      { key: 'nomenclature', label: 'Номенклатура', path: '/catalog/nomenclature' },
      { key: 'suppliers', label: 'Поставщики', path: '/catalog/suppliers' },
      { key: 'contractors', label: 'Подрядчики', path: '/catalog/contractors' },
    ],
  },
  {
    key: 'bom',
    label: 'Структуры изделий',
    icon: <ApartmentOutlined />,
    path: '/bom',
    roles: ['admin', 'engineer'],
  },
  {
    key: 'procurement',
    label: 'Закупки',
    icon: <ShoppingCartOutlined />,
    path: '/procurement',
    roles: ['admin', 'procurement'],
    badge: 'dot',              // Есть просроченные
  },
  {
    key: 'production',
    label: 'Производство',
    icon: <ToolOutlined />,
    path: '/production/orders',
    roles: ['admin', 'production', 'project_manager'],
  },
  {
    key: 'inventory',
    label: 'Склад',
    icon: <InboxOutlined />,
    path: '/inventory',
    roles: ['admin', 'warehouse', 'procurement'],
  },
  {
    key: 'analytics',
    label: 'Аналитика',
    icon: <BarChartOutlined />,
    path: '/analytics/reports',
    roles: ['admin', 'project_manager'],
  },
  // Separator
  {
    key: 'settings',
    label: 'Настройки',
    icon: <SettingOutlined />,
    roles: ['admin'],
    children: [
      { key: 'users', label: 'Пользователи', path: '/settings/users' },
      { key: 'roles', label: 'Роли', path: '/settings/roles' },
      { key: 'system', label: 'Система', path: '/settings/system' },
    ],
  },
];
```

---

## 6. Экраны системы

### 6.1 Dashboard — Панель управления

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           ПАНЕЛЬ УПРАВЛЕНИЯ                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐           │
│  │ АКТИВНЫХ    │ │ ВЫПОЛНЕНИЕ  │ │ ЗАКУПКИ     │ │ ПРОСРОЧЕНО  │           │
│  │ ПРОЕКТОВ    │ │ (СРЕДН.)    │ │ В ПРОЦЕССЕ  │ │             │           │
│  │     3       │ │   67.4%     │ │     47      │ │     12      │           │
│  │  ▲ +1       │ │  ▲ +2.3%    │ │  ▼ -5       │ │  ▲ +3       │           │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘           │
│                                                                             │
│  ┌────────────────────────────────────┐ ┌─────────────────────────────────┐│
│  │      ПРОГРЕСС ПО ПРОЕКТАМ          │ │   КРИТИЧЕСКИЕ ЗАДЕРЖКИ         ││
│  │ ──────────────────────────────────  │ │                                 ││
│  │                                     │ │  ⚠ СТЕНД-001 / ГДС-03          ││
│  │  СТЕНД-001    ████████████░░  78%  │ │    Материал: Сталь 12Х18Н10Т   ││
│  │  СТЕНД-002    ██████░░░░░░░░  45%  │ │    Просрочено: 5 дней          ││
│  │  СТЕНД-003    ███░░░░░░░░░░░  23%  │ │    Влияние: блокирует 12 задач ││
│  │                                     │ │                                 ││
│  │  [Подробнее]                        │ │  ⚠ СТЕНД-002 / СУ-01           ││
│  └────────────────────────────────────┘ │    Подрядчик: ООО "Механика"    ││
│                                          │    Просрочено: 3 дня            ││
│  ┌────────────────────────────────────┐ │                                 ││
│  │      БЛИЖАЙШИЕ ДЕДЛАЙНЫ            │ │  [Все задержки (12)]            ││
│  │ ──────────────────────────────────  │ └─────────────────────────────────┘│
│  │                                     │                                    │
│  │  15.01  СТЕНД-001 / Система ЭНС    │ ┌─────────────────────────────────┐│
│  │  18.01  СТЕНД-002 / Подсистема ГК  │ │   АКТИВНОСТЬ ЗА НЕДЕЛЮ         ││
│  │  22.01  СТЕНД-001 / Поставка мат.  │ │                                 ││
│  │  25.01  СТЕНД-003 / КД утверждено  │ │      📊 [График активности]     ││
│  │                                     │ │                                 ││
│  │  [Все дедлайны]                     │ │                                 ││
│  └────────────────────────────────────┘ └─────────────────────────────────┘│
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**Компоненты Dashboard:**
- `StatCard` — KPI карточки с трендом
- `ProjectProgressList` — список проектов с прогресс-барами
- `CriticalDelaysList` — критические задержки
- `UpcomingDeadlines` — ближайшие дедлайны
- `ActivityChart` — график активности

### 6.2 Project Detail — Карточка проекта

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 📍 Проекты › СТЕНД-001                                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  СТЕНД-001 — Испытательный стенд системы ГДС              ● В работе       │
│  ─────────────────────────────────────────────────────────────────────────  │
│  Прогресс: ████████████████████░░░░░░░░ 67.4%                              │
│  План: 01.10.2025 – 28.02.2026    |    Осталось: 52 дня                    │
│                                                                             │
│  [Обзор] [Структура] [Gantt] [Закупки] [Производство] [Документы] [История]│
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─ СТРУКТУРА ПРОЕКТА ──────────────────────────────────────────────────┐  │
│  │                                                                       │  │
│  │  🔍 Поиск...                              [Развернуть всё] [Фильтры] │  │
│  │                                                                       │  │
│  │  ▼ ◉ Система ГДС                                    ████████ 78%     │  │
│  │    ├─▼ ◉ Подсистема ГДС-01                          ██████░░ 65%     │  │
│  │    │   ├── ◉ СБ.001 Рама основная        ✓ Изгот.   ████████ 100%    │  │
│  │    │   ├── ◉ СБ.002 Кронштейн            ⚙ В работе ██████░░ 70%     │  │
│  │    │   ├── ○ Д.001 Пластина              ○ Не нач.  ░░░░░░░░ 0%      │  │
│  │    │   └── ○ М.001 Сталь 12Х18Н10Т       ⚠ Задержка ░░░░░░░░ 0%      │  │
│  │    ├─▼ ◉ Подсистема ГДС-02                          ████████ 90%     │  │
│  │    │   └── ...                                                       │  │
│  │    └─▸ ◉ Подсистема ГДС-03                          ██░░░░░░ 25%     │  │
│  │                                                                       │  │
│  │  ▶ Система ЭНС                                      ████░░░░ 55%     │  │
│  │  ▶ Система СУ                                       ██░░░░░░ 30%     │  │
│  │                                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌─ ВЫБРАНО: СБ.002 Кронштейн ───────────────────────────────────────────┐ │
│  │                                                                        │ │
│  │  Тип: Сборочная единица        Чертёж: ГДС.01.002                     │ │
│  │  Статус: ⚙ В процессе изготовления                                    │ │
│  │  Исполнитель: Своими силами    Ответственный: Иванов И.И.             │ │
│  │                                                                        │ │
│  │  План:    15.12.2025 – 20.01.2026                                     │ │
│  │  Факт:    18.12.2025 – ...                 Отклонение: +3 дня         │ │
│  │                                                                        │ │
│  │  Материалы: 3 из 5 в наличии                                          │ │
│  │  Комментарий: Ожидание поставки крепежа                               │ │
│  │                                                                        │ │
│  │  [Редактировать] [История изменений] [Добавить комментарий]           │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 6.3 Procurement List — Ведомость закупок

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 📍 Закупки                                                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ВЕДОМОСТЬ ЗАКУПОК                                                          │
│  ─────────────────────────────────────────────────────────────────────────  │
│                                                                             │
│  🔍 Поиск по наименованию, артикулу...                                     │
│                                                                             │
│  Проект: [Все        ▼]  Статус: [Все        ▼]  Поставщик: [Все       ▼] │
│  Тип:    [Все        ▼]  Срочность: [Все     ▼]  □ Только просроченные    │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │ #  │ Наименование          │ Проект   │ Кол-во │ Статус    │ Срок    ││ │
│  ├────┼───────────────────────┼──────────┼────────┼───────────┼─────────┤│ │
│  │ 1  │ Сталь 12Х18Н10Т       │ СТЕНД-001│ 150 кг │ ⚠ Задержка│ 05.01 ❗││ │
│  │ 2  │ Болт М12х50 ГОСТ...   │ СТЕНД-001│ 200 шт │ 🚚 В пути │ 12.01   ││ │
│  │ 3  │ Подшипник 6205-2RS    │ СТЕНД-002│ 8 шт   │ ⏳ Заказано│ 15.01   ││ │
│  │ 4  │ Кабель КГ 3х2.5       │ СТЕНД-001│ 50 м   │ ○ Ожидает │ 18.01   ││ │
│  │ 5  │ Редуктор Ц2У-160      │ СТЕНД-003│ 2 шт   │ ✓ Доставл.│ —       ││ │
│  │ ...│                       │          │        │           │         ││ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  Показано 1-50 из 247    [◀] [1] [2] [3] ... [5] [▶]    [Экспорт Excel]    │
│                                                                             │
│  ─────────────────────────────────────────────────────────────────────────  │
│  СТАТИСТИКА                                                                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │Всего: 247│ │Заказано:47│ │В пути: 23│ │Доставл:165│ │Просроч:12│          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 6.4 Gantt View — Диаграмма Ганта

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 📍 Проекты › СТЕНД-001 › Gantt                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  [День] [Неделя] [Месяц]        ◀ Январь 2026 ▶        [Сегодня] [Фильтры] │
│                                                                             │
│  ┌────────────────────────┬─────────────────────────────────────────────┐  │
│  │ Задача                  │ 01  05  10  15  20  25  30  05  10  15     │  │
│  ├────────────────────────┼─────────────────────────────────────────────┤  │
│  │ ▼ Система ГДС          │ ████████████████████████████████░░░░░░░░░░ │  │
│  │   ├ ГДС-01             │   ████████████████████░░░░░░░░░░░          │  │
│  │   │  ├ СБ.001 Рама     │   ████████████ ✓                           │  │
│  │   │  ├ СБ.002 Кронштейн│        ██████████████████                  │  │
│  │   │  └ Д.001 Пластина  │              ░░░░░░░░░░░░░░                │  │
│  │   ├ ГДС-02             │                    ████████████████        │  │
│  │   └ ГДС-03             │                              ████████████  │  │
│  │                        │                    │                        │  │
│  │ ▼ Система ЭНС          │        ████████████████████████████████    │  │
│  │   └ ...                │                    │                        │  │
│  │                        │                    │                        │  │
│  │ ▶ Система СУ           │                    ████████████████████    │  │
│  └────────────────────────┴────────────────────│────────────────────────┘  │
│                                                │                            │
│  ═══ План   ███ Факт (в срок)   ███ Факт (просрочено)   │ Сегодня         │
│                                                                             │
│  Критический путь: Система ГДС → ГДС-03 → М.001 Сталь → Поставка (05.01)   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 7. Паттерны взаимодействия

### 7.1 Inline Editing

```typescript
// Редактирование прямо в таблице без модальных окон
<EditableCell
  value={row.plannedEndDate}
  onSave={(newValue) => updateItem(row.id, { plannedEndDate: newValue })}
  renderView={(value) => formatDate(value)}
  renderEdit={(value, onChange) => (
    <DatePicker value={value} onChange={onChange} />
  )}
/>
```

### 7.2 Optimistic Updates

```typescript
// Мгновенный отклик UI + синхронизация с сервером
const updateStatus = useMutation({
  mutationFn: (data) => api.updateItemStatus(data),
  onMutate: async (newData) => {
    // Отменяем текущие запросы
    await queryClient.cancelQueries(['items', itemId]);
    
    // Сохраняем предыдущее значение
    const previous = queryClient.getQueryData(['items', itemId]);
    
    // Оптимистично обновляем
    queryClient.setQueryData(['items', itemId], (old) => ({
      ...old,
      ...newData,
    }));
    
    return { previous };
  },
  onError: (err, newData, context) => {
    // Откатываем при ошибке
    queryClient.setQueryData(['items', itemId], context.previous);
    notification.error({ message: 'Ошибка сохранения' });
  },
  onSettled: () => {
    // Инвалидируем для получения актуальных данных
    queryClient.invalidateQueries(['items', itemId]);
  },
});
```

### 7.3 Batch Actions

```typescript
// Массовые действия над выбранными элементами
<BatchActionsBar
  selectedCount={selectedRows.length}
  actions={[
    {
      key: 'setStatus',
      label: 'Изменить статус',
      icon: <EditOutlined />,
      onClick: () => openBatchStatusModal(selectedRows),
    },
    {
      key: 'setResponsible',
      label: 'Назначить ответственного',
      icon: <UserOutlined />,
      onClick: () => openBatchResponsibleModal(selectedRows),
    },
    {
      key: 'export',
      label: 'Экспорт',
      icon: <ExportOutlined />,
      onClick: () => exportSelected(selectedRows),
    },
  ]}
  onClear={() => setSelectedRows([])}
/>
```

### 7.4 Quick Filters (Preset)

```typescript
// Быстрые предустановленные фильтры
const quickFilters = [
  { key: 'overdue', label: 'Просроченные', filter: { status: 'delayed' } },
  { key: 'today', label: 'На сегодня', filter: { dueDate: today } },
  { key: 'myTasks', label: 'Мои задачи', filter: { responsible: currentUserId } },
  { key: 'critical', label: 'Критический путь', filter: { isCritical: true } },
];
```

---

## 8. Real-time обновления

### 8.1 WebSocket Events

```typescript
// Типы событий
type WebSocketEvent =
  | { type: 'PROJECT_UPDATED'; payload: { projectId: string; changes: Partial<Project> } }
  | { type: 'ITEM_STATUS_CHANGED'; payload: { itemId: string; status: Status } }
  | { type: 'DELIVERY_STATUS_CHANGED'; payload: { orderId: string; status: DeliveryStatus } }
  | { type: 'PROGRESS_CALCULATED'; payload: { projectId: string; progress: number } }
  | { type: 'COMMENT_ADDED'; payload: { entityType: string; entityId: string; comment: Comment } };

// Обработка
const useWebSocketSync = () => {
  const queryClient = useQueryClient();
  
  useEffect(() => {
    const ws = new WebSocket(WS_URL);
    
    ws.onmessage = (event) => {
      const data = JSON.parse(event.data) as WebSocketEvent;
      
      switch (data.type) {
        case 'PROJECT_UPDATED':
          queryClient.invalidateQueries(['projects', data.payload.projectId]);
          break;
        case 'ITEM_STATUS_CHANGED':
          // Обновляем конкретный элемент без полной перезагрузки
          queryClient.setQueryData(['items', data.payload.itemId], (old) => ({
            ...old,
            status: data.payload.status,
          }));
          break;
        // ...
      }
    };
    
    return () => ws.close();
  }, [queryClient]);
};
```

### 8.2 Live Indicators

```typescript
// Индикаторы реального времени
<LiveIndicator
  status={itemStatus}
  lastUpdate={lastUpdateTime}
  showPulse={isRecentlyChanged}
/>
```

---

## 9. Визуальный стиль

### 9.1 Цветовая палитра

```css
:root {
  /* Brand */
  --color-primary: #1890ff;
  --color-primary-hover: #40a9ff;
  --color-primary-active: #096dd9;
  
  /* Semantic */
  --color-success: #52c41a;
  --color-warning: #faad14;
  --color-error: #ff4d4f;
  --color-info: #1890ff;
  
  /* Neutral */
  --color-text-primary: #262626;
  --color-text-secondary: #595959;
  --color-text-tertiary: #8c8c8c;
  --color-text-disabled: #bfbfbf;
  
  --color-bg-base: #ffffff;
  --color-bg-layout: #f5f5f5;
  --color-bg-container: #ffffff;
  --color-bg-elevated: #ffffff;
  
  --color-border: #d9d9d9;
  --color-border-secondary: #f0f0f0;
  
  /* Shadows */
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.03);
  --shadow-md: 0 2px 8px 0 rgba(0, 0, 0, 0.08);
  --shadow-lg: 0 4px 16px 0 rgba(0, 0, 0, 0.12);
}
```

### 9.2 Типографика

```css
:root {
  /* Font family */
  --font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 
                 'Helvetica Neue', Arial, sans-serif;
  --font-family-mono: 'SF Mono', Consolas, 'Liberation Mono', Menlo, monospace;
  
  /* Font sizes */
  --font-size-xs: 12px;
  --font-size-sm: 13px;
  --font-size-base: 14px;
  --font-size-lg: 16px;
  --font-size-xl: 18px;
  --font-size-2xl: 20px;
  --font-size-3xl: 24px;
  
  /* Line heights */
  --line-height-tight: 1.25;
  --line-height-base: 1.5;
  --line-height-relaxed: 1.75;
  
  /* Font weights */
  --font-weight-normal: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 600;
  --font-weight-bold: 700;
}

/* Размеры для таблиц */
.table-cell {
  font-size: var(--font-size-sm);  /* 13px для плотности */
  line-height: var(--line-height-tight);
}
```

### 9.3 Отступы и сетка

```css
:root {
  /* Spacing scale */
  --spacing-0: 0;
  --spacing-1: 4px;
  --spacing-2: 8px;
  --spacing-3: 12px;
  --spacing-4: 16px;
  --spacing-5: 20px;
  --spacing-6: 24px;
  --spacing-8: 32px;
  --spacing-10: 40px;
  --spacing-12: 48px;
  
  /* Layout */
  --sidebar-width: 240px;
  --sidebar-collapsed-width: 64px;
  --header-height: 56px;
  --content-padding: 24px;
  --border-radius: 6px;
}
```

### 9.4 Ant Design Theme Override

```typescript
// src/styles/theme.ts
import { ThemeConfig } from 'antd';

export const theme: ThemeConfig = {
  token: {
    // Colors
    colorPrimary: '#1890ff',
    colorSuccess: '#52c41a',
    colorWarning: '#faad14',
    colorError: '#ff4d4f',
    colorInfo: '#1890ff',
    
    // Typography
    fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif',
    fontSize: 14,
    fontSizeSM: 13,
    fontSizeLG: 16,
    
    // Layout
    borderRadius: 6,
    borderRadiusSM: 4,
    borderRadiusLG: 8,
    
    // Spacing
    padding: 16,
    paddingSM: 12,
    paddingLG: 24,
    margin: 16,
    marginSM: 12,
    marginLG: 24,
    
    // Compact mode for data-heavy interfaces
    controlHeight: 32,
    controlHeightSM: 24,
    controlHeightLG: 40,
  },
  components: {
    Table: {
      cellPaddingBlock: 8,
      cellPaddingInline: 12,
      headerBg: '#fafafa',
      rowHoverBg: '#f5f5f5',
    },
    Menu: {
      itemHeight: 40,
      subMenuItemBg: 'transparent',
    },
    Card: {
      paddingLG: 20,
    },
  },
};
```

---

## 10. Performance

### 10.1 Виртуализация таблиц

```typescript
// Для таблиц > 100 строк
import { useVirtualizer } from '@tanstack/react-virtual';

const VirtualTable = ({ data, columns }) => {
  const parentRef = useRef<HTMLDivElement>(null);
  
  const virtualizer = useVirtualizer({
    count: data.length,
    getScrollElement: () => parentRef.current,
    estimateSize: () => 40, // row height
    overscan: 10,
  });
  
  return (
    <div ref={parentRef} style={{ height: '600px', overflow: 'auto' }}>
      <div style={{ height: `${virtualizer.getTotalSize()}px` }}>
        {virtualizer.getVirtualItems().map((virtualRow) => (
          <TableRow
            key={virtualRow.key}
            style={{
              position: 'absolute',
              top: virtualRow.start,
              height: virtualRow.size,
            }}
            data={data[virtualRow.index]}
            columns={columns}
          />
        ))}
      </div>
    </div>
  );
};
```

### 10.2 Lazy Loading

```typescript
// Ленивая загрузка модулей
const ProjectDetailPage = lazy(() => import('./pages/projects/ProjectDetailPage'));
const BOMEditorPage = lazy(() => import('./pages/bom/BOMEditorPage'));
const GanttView = lazy(() => import('./components/GanttView'));

// Использование
<Suspense fallback={<PageLoader />}>
  <ProjectDetailPage />
</Suspense>
```

### 10.3 Query Prefetching

```typescript
// Предзагрузка данных при наведении
const ProjectListItem = ({ project }) => {
  const queryClient = useQueryClient();
  
  const handleMouseEnter = () => {
    queryClient.prefetchQuery({
      queryKey: ['project', project.id],
      queryFn: () => api.getProject(project.id),
      staleTime: 60000,
    });
  };
  
  return (
    <Link
      to={`/projects/${project.id}`}
      onMouseEnter={handleMouseEnter}
    >
      {project.name}
    </Link>
  );
};
```

---

## 11. Доступность (A11y)

### 11.1 Keyboard Navigation

```typescript
// Навигация по таблице клавиатурой
const useTableKeyboardNav = (rows: any[], onSelect: (row: any) => void) => {
  const [focusedIndex, setFocusedIndex] = useState(0);
  
  useEffect(() => {
    const handleKeyDown = (e: KeyboardEvent) => {
      switch (e.key) {
        case 'ArrowDown':
          setFocusedIndex((i) => Math.min(i + 1, rows.length - 1));
          break;
        case 'ArrowUp':
          setFocusedIndex((i) => Math.max(i - 1, 0));
          break;
        case 'Enter':
          onSelect(rows[focusedIndex]);
          break;
      }
    };
    
    document.addEventListener('keydown', handleKeyDown);
    return () => document.removeEventListener('keydown', handleKeyDown);
  }, [rows, focusedIndex, onSelect]);
  
  return { focusedIndex };
};
```

### 11.2 Focus Management

```typescript
// Управление фокусом в модальных окнах
const FocusTrap = ({ children, active }) => {
  const containerRef = useRef<HTMLDivElement>(null);
  
  useEffect(() => {
    if (active) {
      const firstFocusable = containerRef.current?.querySelector<HTMLElement>(
        'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
      );
      firstFocusable?.focus();
    }
  }, [active]);
  
  return <div ref={containerRef}>{children}</div>;
};
```

---

## 12. Testing Strategy

### 12.1 Unit Tests

```typescript
// Vitest + React Testing Library
describe('StatusBadge', () => {
  it('renders correct color for delayed status', () => {
    render(<StatusBadge status="delayed" />);
    expect(screen.getByRole('status')).toHaveStyle({ color: '#ff4d4f' });
  });
  
  it('shows pulsating animation for critical statuses', () => {
    render(<StatusBadge status="delayed" pulsate />);
    expect(screen.getByRole('status')).toHaveClass('pulsate');
  });
});
```

### 12.2 Integration Tests

```typescript
// Тесты с MSW для мокирования API
describe('ProjectList', () => {
  it('loads and displays projects', async () => {
    server.use(
      rest.get('/api/v1/projects', (req, res, ctx) => {
        return res(ctx.json({ results: mockProjects }));
      })
    );
    
    render(<ProjectListPage />);
    
    await waitFor(() => {
      expect(screen.getByText('СТЕНД-001')).toBeInTheDocument();
    });
  });
});
```

### 12.3 E2E Tests

```typescript
// Playwright
test('user can change item status', async ({ page }) => {
  await page.goto('/projects/1');
  
  // Находим элемент в структуре
  await page.click('text=СБ.001 Рама основная');
  
  // Меняем статус
  await page.click('[data-testid="status-select"]');
  await page.click('text=Изготовлено');
  
  // Проверяем обновление
  await expect(page.locator('[data-testid="status-badge"]')).toHaveText('Изготовлено');
});
```

---

## 13. Deployment

### 13.1 Build Configuration

```typescript
// vite.config.ts
export default defineConfig({
  build: {
    target: 'es2020',
    outDir: 'dist',
    sourcemap: true,
    rollupOptions: {
      output: {
        manualChunks: {
          'vendor-react': ['react', 'react-dom', 'react-router-dom'],
          'vendor-antd': ['antd', '@ant-design/icons'],
          'vendor-charts': ['recharts', '@ant-design/plots'],
          'vendor-utils': ['date-fns', 'lodash-es', 'axios'],
        },
      },
    },
  },
});
```

### 13.2 Environment Variables

```bash
# .env.production
VITE_API_URL=https://api.pdm.company.local/api/v1
VITE_WS_URL=wss://api.pdm.company.local/ws
VITE_APP_TITLE=PDM ERP
```

---

## 14. Рекомендации по развитию

### Фаза 1 (MVP)
- [ ] Авторизация
- [ ] Dashboard (базовый)
- [ ] Список проектов
- [ ] Карточка проекта со структурой
- [ ] Справочники (номенклатура, поставщики, подрядчики)

### Фаза 2
- [ ] BOM редактор
- [ ] Ведомость закупок
- [ ] Ведомость производства
- [ ] Детальные статусы и даты

### Фаза 3
- [ ] Gantt диаграммы
- [ ] WebSocket live-updates
- [ ] Расширенная аналитика
- [ ] Экспорт отчётов

### Фаза 4
- [ ] Настраиваемые дашборды
- [ ] Уведомления
- [ ] Интеграции
- [ ] Мобильная версия (read-only)

---

*Документ создан: Январь 2026*
*Версия: 1.0*
